{
  "name": "surveyapp",
  "version": "1.0.0",
  "description": "Anonymous survey where each participant answers one randomly assigned question, with an admin panel and Excel export.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/index.js",
    "test": "node --disable-warning=ExperimentalWarning --test \"test/**/*.test.js\""
  },
  "license": "MIT",
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^5.2.1"
  }
}
